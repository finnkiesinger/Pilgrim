<GUI>
    <Panel>

    </Panel>
</GUI>

<!--
Every Component on the root level (inside the GUI tags) must have an absolute position
This is done using constraints.
The width of a component is then inferred based on these constraints
It can also be specified manually.
Constraints rank higher

Constraints > Position

If a component has a width of
-->